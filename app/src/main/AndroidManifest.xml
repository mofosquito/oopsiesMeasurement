<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ucts.oopsiesmeasurement">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OopsiesMeasurement">
        <activity android:name=".source_code"></activity>
        <activity android:name=".feedback" />
        <activity android:name=".about_us" />
        <activity android:name=".heat_temp" />
        <activity android:name=".estimating_quantities" />
        <activity android:name=".length_mass_time" />
        <activity android:name=".phenomena_illusions" />
        <activity android:name=".Settings" />
        <activity android:name=".Fun_Facts" />
        <activity android:name=".Educational" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>